# Legal Translator Frontend

React frontend for the Legal Document Translator application.

## Setup

```bash
cd frontend
npm install
```

## Development

```bash
npm run dev
```

The frontend will run on http://localhost:3000 and proxy API requests to the backend at http://localhost:8000.

## Build

```bash
npm run build
```

The built files will be in the `dist` directory.






